name: "Bug Report - Linux USB Creation"
description: "Report issues related to Linux image analysis and USB creation."
title: "[Linux USB Creation] "
labels: ["bug", "usb-creation", "linux"]
body:
  - type: markdown
    attributes:
      value: |
        Please fill in all required fields.

  - type: input
    id: app_version
    attributes:
      label: "macUSB version"
      placeholder: "e.g., v2.1"
    validations:
      required: true

  - type: input
    id: host_os_version
    attributes:
      label: "Host macOS version"
      placeholder: "e.g., macOS Sequoia 15.4"
    validations:
      required: true

  - type: dropdown
    id: host_architecture
    attributes:
      label: "Host architecture"
      options:
        - "Apple Silicon"
        - "Intel"
    validations:
      required: true

  - type: checkboxes
    id: required_permissions
    attributes:
      label: "Required permissions"
      options:
        - label: "I enabled Full Disk Access for macUSB in System Settings"
          required: true
        - label: "I enabled Allow in the Background for macUSB in System Settings"
          required: true

  - type: dropdown
    id: helper_repair
    attributes:
      label: "Was helper repair executed? (Tools -> Repair helper)"
      options:
        - "Yes"
        - "No"
        - "Not applicable"
    validations:
      required: true

  - type: input
    id: target_distribution
    attributes:
      label: "Which Linux distribution were you creating?"
      placeholder: "e.g., Ubuntu 26.04 / Fedora Workstation 42 / Arch Linux"
    validations:
      required: true

  - type: dropdown
    id: installer_format
    attributes:
      label: "Image format"
      options:
        - ".iso"
        - ".cdr"
    validations:
      required: true

  - type: textarea
    id: installer_source_details
    attributes:
      label: "Image source details"
      description: "Where does the image come from? Add link/source details."
      placeholder: "e.g., Official Ubuntu download"
    validations:
      required: true

  - type: dropdown
    id: failure_stage
    attributes:
      label: "Failure stage"
      options:
        - "Analysis"
        - "USB creation"
        - "Other"
    validations:
      required: true

  - type: textarea
    id: problem_description
    attributes:
      label: "Problem description"
      placeholder: "Describe the issue in a few sentences."
    validations:
      required: true

  - type: dropdown
    id: media_type
    attributes:
      label: "Target media type"
      options:
        - "Pendrive"
        - "Hard drive"
        - "Not applicable"
    validations:
      required: true

  - type: dropdown
    id: reproducibility
    attributes:
      label: "Is the issue reproducible?"
      options:
        - "Always"
        - "Sometimes"
        - "Happened once"
    validations:
      required: true

  - type: textarea
    id: repro_steps
    attributes:
      label: "Steps to reproduce"
      placeholder: |
        1. Open macUSB
        2. Select Linux image ...
        3. Select target USB ...
        4. Start process
        5. Observe error
    validations:
      required: true

  - type: upload
    id: diagnostic_logs
    attributes:
      label: "Diagnostic logs"
      description: "Export diagnostic logs using Option (⌥) + L, then attach the exported .txt file."
    validations:
      required: true
      accept: ".txt"

  - type: upload
    id: problem_media
    attributes:
      label: "Screenshots or videos"
      description: "Attach screenshots or a video if they help demonstrate the problem."
    validations:
      required: false
      accept: ".png,.jpg,.jpeg,.gif,.webp,.mp4,.mov,.webm"
